//- mixin을 사용하는 이유
//- 각각 다른 정보를 가지지만 같은 구조를 갖는 데이터를 표시하기위해 코드를 캡슐화함

mixin videoBlock(video = {})
    //- h1= video.title
    //- p= video.description
    .videoBlock
        a(href=routes.videoDetail(video.id))
            video.thum(src=video.videoFile, controls= false, )
            //- poster="https://peach.blender.org/wp-content/uploads/title_anouncement.jpg?x11217"
            h4.title= video.title
            if video.views === 1
                h6.views 1 view
            else
                h6.views #{video.views} views

