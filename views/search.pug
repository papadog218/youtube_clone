extends layouts/main
include mixins/videoBlock

block content
    .search
        header
            h3 Searching for : #{searchingBy}
    .videos
         each item in videos
            +videoBlock({
                title: item.title,
                views: item.views,
                videoFile: item.videoFile
            })