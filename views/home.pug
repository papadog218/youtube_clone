extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        each item in videos
            //- 방법 1.
            //- h1= item.title
            //- p= item.description
            //- 방법 2. mixin
            //- description: item.description
            +videoBlock({
                id: item.id,
                title: item.title,
                views: item.views,
                videoFile: item.fileUrl
            })